# This is a public, open-source repo: always resolve against the public npm registry.
# Without this, a machine configured with a private registry mirror bakes internal
# hostnames into package-lock.json's `resolved` URLs, which both leaks infrastructure
# details and breaks `npm install` for everyone else.
registry=https://registry.npmjs.org/
